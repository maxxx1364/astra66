# HANDOFF — وضعیتِ واقعی و قدم بعدی

**تاریخ:** 2026-09-18 · **شاخه:** `arena/01a0b492-astra66` · **تست‌ها:** ۶۱۷ پاس

> این سند جایگزینِ نسخهٔ 2026-09-17 شد. آن نسخه می‌گفت «۱۰ فایل commit‌نشده» و
> «۵۹۰ تست»؛ هیچ‌کدام درست نبود (واقعیت: ۵۶۲ تست و دو فایل — `real_data_research.py`
> و `test_fetch_binance.py` — که هرگز به مخزن نرسیدند). جلسهٔ بعد را گمراه کرد،
> پس بازنویسی شد. **اعدادِ این سند با اجرا تأیید شده‌اند، نه نقل‌شده از جای دیگر.**

---

## ۱. کجا هستیم

فاز ۱ (موتور) و فاز ۳ (بک‌تست استراتژی) تمام‌اند. ما در آغازِ **فاز ۳.۵
(اعتبارسنجی)** هستیم و مسئله دیگر نبودِ داده نیست — کافی‌نبودنِ نمونه و غلبهٔ
هزینه است.

| بررسی | مقدار |
|---|---|
| `python3 -m pytest` | **۶۱۷ تست پاس** (≈ ۳۰ ثانیه) در ۹ فایل |
| خطاهای حسابداری روی دادهٔ واقعی | **۰** در همهٔ اجراها |
| پوششِ دادهٔ واقعی | ۵۲۵٬۶۰۱ دقیقه به‌ازای هر نماد، **۰ دقیقهٔ گم‌شده** |
| `n` (ادغام‌شده، ۴ نماد × ۱ سال) | **۴۲۷** — در برابر ~۵۶۹ لازم برای توانِ ۸۰٪ |
| اصطکاک ÷ لبهٔ ناخالص | **۲.۲۰** |

**خوانش:** ورود لبه دارد (هر ۴ نماد هر دو کنترل را می‌شکنند)، اما اصطکاک ۲.۲
برابرِ کل لبه است و نمونه هنوز زیر آستانهٔ توان است. این «سربه‌سر» نیست؛ منفیِ
قاطع با اعتبارِ آماریِ ناکافی است.

---

## ۲. زیرساختِ داده (تغییرِ اصلی این دوره)

`research-real` پیش از این هر بار کل تاریخ را از نو می‌ساخت (~۳۳ دقیقه، که
تقریباً همه تأخیرِ درخواست بود نه محدودیتِ نرخ — ۵۲۶ درخواست ≈ ۱۰۵۲ وزن در برابر
سقفِ ۶۰۰۰ در دقیقه). حالا:

1. **کشِ Actions** فروشگاه را برمی‌گرداند؛ روی کشِ سرد از releaseِ `klines` seed
   می‌خورد (`data-store.yml` آن را می‌سازد و **پیش از انتشار** از نظر حفره و
   اعتبارِ OHLC بررسی می‌کند).
2. **`--append`** فقط بلوکِ چسبیده به آخرین میلهٔ ذخیره‌شده را می‌گیرد؛ روی کشِ
   گرم مرحلهٔ دانلود عملاً هیچ کاری نمی‌کند.
3. **آرشیو vision** مقدم است: یک zip برای هر ماه با SHA256 منتشرشده (~۳۶ درخواست
   برای سه سال به‌جای ~۱۶۰۰)؛ REST روی میرور پشتیبان است.
4. فروشگاه یک **پنجرهٔ غلتان** است و هنگام نوشتن بریده می‌شود، پس تا طولِ تاریخ
   رشد می‌کند و همان‌جا می‌ماند.

نکتهٔ غیربدیهی: `--store` برای `--append` **ضروری** است. نامِ پیش‌فرضِ فایل
تاریخِ پایانِ پنجره را هم دارد و `--days` انتها را «الان» می‌گیرد، پس نام هر اجرا
عوض می‌شد و اجرای بعدی فروشگاه را پیدا نمی‌کرد.

---

## ۳. مدل هزینه

`adv_notional = 2e8` یک ثابتِ hard-code بود که برای همهٔ نمادها یکسان اعمال
می‌شد — یعنی یک جفتِ کم‌عمق به‌اندازهٔ عمیق‌ترین جفتِ صرافی نقد شمرده می‌شد. روی
مقیاس‌های واقعی اختلاف ~۶ برابر است، یعنی بزرگ‌تر از کل لبهٔ ناخالص.

حالا `engine/venues.py` دو مقدار را از خودِ داده می‌سنجد و بقیه را فرض می‌گذارد:

| مقدار | اندازه‌گیری / فرض |
|---|---|
| گامِ قیمتی، عمق (ADV) | **اندازه‌گیری** از داده |
| کارمزدِ taker/maker | **فرض** — به سطحِ VIP حساب بستگی دارد، نه بازار |
| لغزش | اعلامی، مگر اینکه گامِ قیمتی کفِ بالاتری تحمیل کند |

هر بک‌تست در `manifest` (`costs` و `cost_calibration`) ثبت می‌کند چه چیزی
اندازه‌گیری شده و چه چیزی فرض شده. **دادهٔ مصنوعی عمداً دست‌نخورده می‌ماند** تا
مبنای مطالعهٔ مصنوعی جابه‌جا نشود.

---

## ۴. بایدها و نبایدهایی که با هزینه یاد گرفته شد

* این سندباکس **هیچ راهی به شبکه ندارد** (هر چهار میزبان در handshake ی TLS شکست
  می‌خورند). فقط رانرِ CI می‌تواند دانلود کند، پس هر تستِ مربوط به دانلود باید
  آفلاین باشد.
* `HANDOFF` جایگزینِ تست نیست. ادعایِ «۵۹۰ تست» در حالی که ۵۶۲ تا وجود داشت،
  چون کسی اجرا نگرفته بود.
* `sweep` و `ablation` تا همین دوره **کانفیگِ ریسک را پاس نمی‌دادند**، پس
  ردیف‌های منتشرشدهٔ جاروب با تنظیماتِ هزینهٔ CLI ساخته نشده بودند — و جاروب یکی
  از نتایجِ اصلی است. بعد از هر تغییر در کانفیگ، بررسی کن که **همهٔ** فرمان‌ها آن
  را دریافت می‌کنند.
* پشتیبانی `.csv.gz` در `CsvFeed` که نسخهٔ قبلی وعده داده بود، هیچ‌وقت به مخزن
  نرسیده بود.

---

## ۵. قدم بعدی (به ترتیب اولویت)

1. **ادغامِ PR #3** و اجرای `data-store.yml` (ساختِ فروشگاهِ ۳ ساله) و سپس
   `backtest.yml` با universe ی BTC/ETH/XRP/SOL × ۳ سال. انتظار: `n` از ۴۲۷ به
   حدود ~۱۲۰۰ — بالاخره بالای آستانهٔ توان.
2. **حمله به اصطکاک، نه به سیگنال.** نسبتِ ۲.۲ می‌گوید درمان هندسی/اجرایی است:
   استاپِ عریضه‌تر، سهمِ maker بیشتر، پایِ taker کمتر. `friction_per_r()` در
   `venues.py` واحدِ درست برای سنجشِ این است (همان ۶bp می‌تواند ۰.۰۳R یا ۰.۱۲R
   باشد، بسته به عرضِ استاپ).
3. **بازتولید صادقانهٔ `final_profitable_system.py`** — هنوز شروع نشده. یافته‌های
   مرورِ قبلی (fill ناممکن روی `entry+1.5R`، look-ahead در صدکِ
   `capital_ratio`، ترتیبِ درون‌میله‌ایِ trailing، پایِ پیرامیدِ بی‌استاپ، نقضِ
   هویتِ `qty × stop_distance == risk_cash`) در نسخهٔ 2026-09-17 این سند بود و
   در بازنویسی حذف شد؛ از تاریخچهٔ git بیاوریدش اگر این کار شروع شد.
4. **تصمیم‌های باز:** کانفیگ‌های Next.js در ریشهٔ مخزن (قالبِ
   `nextjs-postgresql-template`، بی‌ربط به موتور پایتونی) حذف شوند یا نه؛ و
   نگه‌داریِ `tbae/docs/results/` (الآن ۱.۱ مگابایت و با هر اجرای ۴نمادی ~۳۵۰
   کیلوبایت رشد می‌کند).

---

## ۶. راستی‌آزمایی

```bash
cd tbae
python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
python3 -m pytest -q                                  # انتظار: 617 passed

python3 scripts/fetch_binance.py --symbols XRPUSDT --days 1095 \
        --store --append --gzip --out-dir data/binance
python3 -m app.cli compare --feed csv:data/binance/XRPUSDT_1m.csv.gz --tf 60
```
