[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    slow: runs a full pipeline/backtest (seconds, not milliseconds)
    api: exercises the FastAPI surface via TestClient
filterwarnings =
    error::DeprecationWarning:engine.*
    error::DeprecationWarning:app.*
